//! Child thread orchestration — mirrors original `app-store-orchestration.ts`.
//! Full agent-forks and supervision loops are complex async flows.
//! Currently stubs; real impl requires AgentSession fork support.

// ponytail: fork_thread/child_supervision - stub, no-op until AgentSession fork lands
