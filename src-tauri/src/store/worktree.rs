//! Git worktree management — mirrors original `worktree-manager.ts` + `app-store-worktree.ts`.
//! Currently stubs; real impl requires `git worktree add` / `git worktree remove`.

// ponytail: create/remove worktree - stub, real impl via std::process::Command when needed
